/**
 * /sessions — No session selected.
 * SessionList is shown as sidebar by the parent layout.
 */
export function SessionsPage() {
  return (
    <div className="flex-1 flex items-center justify-center text-[var(--text-dim)] text-sm">
      セッションを選択してください
    </div>
  )
}
